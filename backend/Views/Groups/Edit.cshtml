@model GroupView

@{
    ViewData["Title"] = "Edit Group";
}

<div class="sa-container">
    <h2>Edit Group</h2>
    <hr/>
    <form asp-action="Edit">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="group.Id"/>
        <div class="form-row">
            <div class="form-group col-12">
                <input asp-for="group.Name" class="form-control" placeholder="Name" required/>
                <span asp-validation-for="group.Name" class="text-danger"></span>
            </div>
            <div class="form-group col-12">
                <label asp-for="@Model.People" class="control-label"></label>
                <select asp-for="@Model.SelectedPeople"
                        class="form-control"
           @model backend.Models.Location

@{
    ViewData["Title"] = "New Location";
}

<div class="sa-container">
    <h2>Create Location</h2>
    <hr/>

    <form asp-action="Create">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-row">
            <div class="form-group col-12">
                <input asp-for="Name" required class="form-control" placeholder="Name"/>
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group col-12">
                <input asp-for="Description" class="form-control" placeholder="Description"/>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <div class="form-group col-6">
                <input asp-for="Latitude" class="form-control" placeholder="Latitude"/>
                <span asp-validation-for="Latitude" class="text-danger"></span>
            </div>
            <div class="form-group col-6">
                <input asp-for="Longitude" class="form-control" placeholder="Longitude"/>
                <span asp-validation-for="Longitude" class="text-danger"></span>
            </div>
            <div class="form-group col-12">
                <div id="leafmap"></div>
            </div>
            <div class="form-group col-12">
                <input type="submit" value="Create" class="btn btn-primary"/>
                <a asp-action="Index" class="btn btn-light">Cancel</a>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
             asp-items="@Model.PeopleList">
                </select>
                <span asp-validation-for="@Model.SelectedPeople" class="text-danger"/>
            </div>
            <div class="form-group col-12">
                <input type="submit" value="Save" class="btn btn-primary"/>
                <a asp-controller="People" asp-action="Index" class="btn btn-light">Cancel</a>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
